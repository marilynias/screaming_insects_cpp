
# Insect Simulation

## Compile

### Requirements:
- cmake
- sdl2


### to build:
```bash
cmake --build .
```

make sure the file is is the same directory s the `/media/` directory and `config.cfg` file

you may edit the config file to your hearts content

## Keybinds
- `+` speed up simulation
- `-` slow down simulation